# 코딩 평가 기준

- 요구 언어·API·입출력 계약을 지키는지와 코드가 실제로 실행 가능한지 확인한다.
- 정상 입력뿐 아니라 명시된 경계값, 빈 입력, 중복, 인덱스와 자원 정리를 검토한다.
- 존재하지 않는 API, 타입·문법 오류와 잘못된 복잡도 설명은 실질 결함으로 본다.
- 더 세련된 구현이라는 이유만으로 정확하게 동작하는 다른 접근을 낮게 평가하지 않는다.
